<!DOCTYPE html>
<html>
<head>
  <title>Profile</title>
</head>
<body>
  <div class="container">
    <h1>Halo, <%= user.displayName %>!</h1>
    <img src="<%= user.photos[0].value %>" width="100" height="100" />
    <p>Nama Lengkap: <%= user.displayName %></p>
    <p>Email: <%= user.emails[0].value %></p>
    <a class="button" href="/logout">Logout</a>
  </div>
</body>
</html>
